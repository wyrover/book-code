#include <iostream>
#include <deque>

int main(int argc, const char *argv[])
{

    // deque (usually pronounced like "deck") is an irregular acronym of double-ended queue. 
    // Double-ended queues are sequence containers with dynamic sizes that can be expanded or contracted on both ends (either its front or its back).
    // Constructor  
    // The constructor is very similar with vector and deque has the similar interface with vector except it can operate on the front
    // In the most cases, deque can be used to simulate stack, queue
    // important method : push_front(), pop_front, push_back, pop_back(), front(), back() 
    // stack definition
    // 
    // template <class _Tp, 
    //           class _Sequence __STL_DEPENDENT_DEFAULT_TMPL(deque<_Tp>) >
    // class stack;
    
    // queue definition
    // 
    // template <class _Tp,
    //           class _Sequence __STL_DEPENDENT_DEFAULT_TMPL(deque<_Tp>) >
    // class queue; 
    
    return 0;
}
