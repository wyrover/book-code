#include "util.h"
#include <stdio.h>


void cactus::hex_print_buffer( buffer *chars )
{
	for(size_t i = 0; i <chars->length; i++)
	{
		printf("%02x", chars->bytes[i]);
		if(i % 40 == 39) {
			putchar('\n');
		}
	}
}
